# Agents Library

Reusable subagent templates.

- `stable/`: safe to install into real projects.
- `experimental/`: test in sandbox projects before use.

Current stable agent:

- `safe-reviewer`: review-only agent for release risk, security, regressions, and verification gaps.
